trigger:
- main

pool:
  name: self-hosted-pool

variables:
  ACR_NAME: sarashcr
  AKS_NAME: aks
  RG: sarash-devops

stages:

# ---------------- BUILD ----------------
- stage: Build
  jobs:
  - job: Build
    steps:
    - script: |
        az login --identity
        az acr login --name $ACR_NAME

        docker build -t $ACR_NAME.azurecr.io/myapp:$(Build.BuildId) .
        docker push $ACR_NAME.azurecr.io/myapp:$(Build.BuildId)
      displayName: Build and Push Image

# ---------------- APPROVAL DEV ----------------
- stage: Approval_Dev
  dependsOn: Build
  jobs:
  - job: Wait
    pool: server
    steps:
    - task: ManualValidation@0
      inputs:
        instructions: "Approve deployment to DEV"

# ---------------- DEPLOY DEV ----------------
- stage: Deploy_Dev
  dependsOn: Approval_Dev
  jobs:
  - job: Deploy
    steps:
    - script: |
        az login --identity
        az aks get-credentials --name $AKS_NAME --resource-group $RG --overwrite-existing

        kubectl create namespace dev --dry-run=client -o yaml | kubectl apply -f -

        # Ensure base resources exist
        kubectl apply -f $(System.DefaultWorkingDirectory)/k8s/dev.yaml
        kubectl apply -f $(System.DefaultWorkingDirectory)/k8s/dev-ingress.yaml

        # 🔥 Update image (THIS triggers rollout automatically)
        kubectl set image deployment/myapp \
          myapp=$ACR_NAME.azurecr.io/myapp:$(Build.BuildId) \
          -n dev
      displayName: Deploy to Dev

# ---------------- APPROVAL PROD ----------------
- stage: Approval_Prod
  dependsOn: Deploy_Dev
  jobs:
  - job: Wait
    pool: server
    steps:
    - task: ManualValidation@0
      inputs:
        instructions: "Approve deployment to PROD"

# ---------------- DEPLOY PROD ----------------
- stage: Deploy_Prod
  dependsOn: Approval_Prod
  jobs:
  - job: Deploy
    steps:
    - script: |
        az login --identity
        az aks get-credentials --name $AKS_NAME --resource-group $RG --overwrite-existing

        kubectl create namespace prod --dry-run=client -o yaml | kubectl apply -f -

        # Ensure base resources exist
        kubectl apply -f $(System.DefaultWorkingDirectory)/k8s/prod.yaml
        kubectl apply -f $(System.DefaultWorkingDirectory)/k8s/prod-ingress.yaml

        # 🔥 Update image (auto rollout)
        kubectl set image deployment/myapp \
          myapp=$ACR_NAME.azurecr.io/myapp:$(Build.BuildId) \
          -n prod
      displayName: Deploy to Prod