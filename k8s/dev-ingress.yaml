apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myapp-dev-ingress
  namespace: dev
spec:
  ingressClassName: azure-application-gateway
  rules:
  - host: dev.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: myapp-service
            port:
              number: 80